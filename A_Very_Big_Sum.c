#include<stdio.h>
int main()
{
    long long int n,i,sum=0;
    scanf("%lli",&n);
    int arr[i];
    for(i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
        sum+=arr[i];
    }
    printf("%lli",sum);
}